package api

import ()

const ()

var ()

// ProxyConfig is the deserialized object from the command-line
type ProxyConfig struct {
}

// StartProxy starts a service proxy
func (a *api) StartProxy(config ProxyConfig) error {

	// Start the shell web server on port 50000
	// Run the subprocess
	// Logstash options
	// Monitor application endpoints
	// Metric forwarder
	return nil
}

func shellServer(inPort, outPort string) {
}
