package cmd

import (
	"fmt"

	"github.com/codegangsta/cli"
)

// CmdProxy is the command-line interaction for serviced proxy.
// usage: serviced proxy
func CmdProxy(c *cli.Context) {
	fmt.Println("serviced proxy ...")
}