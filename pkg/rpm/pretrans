%pretrans -p <lua>
-- https://fedoraproject.org/wiki/Packaging:Directory_Replacement
path="/opt/serviced/share/web/static/lib"
st = posix.stat(path)
if st and st.type == "link" then
  os.remove(path)
end
